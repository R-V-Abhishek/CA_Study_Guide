[project]
name = "caf-l1"
version = "0.1.0"
description = "L1 Acquisition pipeline for ICAI study materials and exam papers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "caf-contracts",
    "caf-common",
    "caf-db",
    "httpx>=0.27",
    "selectolax>=0.3",
    "playwright>=1.40",
]

[tool.uv.sources]
caf-contracts = { workspace = true }
caf-common = { workspace = true }
caf-db = { workspace = true }
