[project]
name = "caf-db"
version = "0.1.0"
description = "Database schema, SQLAlchemy ORM models, and migrations"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "caf-contracts",
    "caf-common",
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "psycopg[binary]>=3.1",
]

[tool.uv.sources]
caf-contracts = { workspace = true }
caf-common = { workspace = true }
